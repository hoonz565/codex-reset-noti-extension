# Risk Register — Codex Reset Notifier

> **Version:** 0.5 (Architecture Correction)  
> **Format:** likelihood (L/M/H), impact (L/M/H/C=Critical)

---

## Risk Matrix

| #   | Risk                                                                                     | L   | Impact | Mitigation                                                                                                                                 | Detection                                                | Recovery                                                                  | Phase    |
| --- | ---------------------------------------------------------------------------------------- | --- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------- | ------------------------------------------------------------------------- | -------- |
| R01 | Upstream `/api/forecast` endpoint is undocumented and may change URL, shape, or add auth | M   | C      | Runtime schema validation; parser failure alert; monitor `script.js` version param                                                         | Schema validation failure logged; empty `forecast` field | Roll back to previous parser version; show sourceHealth=unavailable       | Phase 3  |
| R02 | Upstream schema adds or renames required fields                                          | H   | H      | Zod/manual strict validation with version tagging; parser smoke test in CI                                                                 | Validation failure triggers PARSER_FAILURE event         | Hotfix parser; redeploy Worker                                            | Phase 3  |
| R03 | Source becomes rate-limited (429)                                                        | L   | H      | User-Agent header; 15-min poll cadence; exponential backoff; no retry on 429 within same cycle                                             | HTTP 429 response logged                                 | Wait for next scheduled cycle; do not retry aggressively                  | Phase 3  |
| R04 | Source refresh cadence changes (`nextRefreshAt` shifts)                                  | M   | L      | We do not rely on `nextRefreshAt` for our schedule; our Cron owns the cadence                                                              | Observed staleness in sourceUpdatedAt                    | No action needed; we always poll our own schedule                         | Phase 3  |
| R05 | Cloudflare free-tier limits change (requests, D1 writes)                                 | L   | H      | Document limits; monitor usage; design for efficient writes (meaningful_change filter)                                                     | Usage dashboard alerts                                   | Upgrade to paid plan or optimize further                                  | Phase 8  |
| R06 | D1 feature or transaction limitations block required patterns                            | M   | H      | Verify D1 capabilities before writing schema (Phase 0.5 task); test with wrangler locally                                                  | CI integration test failures                             | Rewrite affected patterns; workaround with application-level logic        | Phase 2  |
| R07 | Cron execution delayed or skipped by Cloudflare platform                                 | M   | M      | This is not a real-time system; document ±15 min accuracy; manual admin trigger available                                                  | Missed event detected via audit_events gap               | Protected `/api/admin/orchestration/run` endpoint                         | Phase 8  |
| R08 | Email provider free-tier quota exhausted (100/day Resend)                                | H   | H      | Per-recipient delivery tracking; retry next day; priority ordering; scale with Resend paid tier                                            | `PROVIDER_QUOTA_EXCEEDED` in delivery records            | Retry; optionally switch provider; inform admin                           | Phase 7  |
| R09 | Sender domain SPF/DKIM not configured; emails go to spam                                 | H   | H      | Document sender domain setup; include List-Unsubscribe; use transactional category                                                         | Subscriber complaints; bounce rate                       | Configure domain properly; re-send if confirmed deliverable               | Phase 11 |
| R10 | Provider accepts email but recipient never receives it                                   | M   | M      | Clearly distinguish `sent_to_provider` from `delivered_to_recipient`; do not falsely claim delivery                                        | No feedback without bounce webhook                       | Add bounce webhook in post-MVP; document limitation                       | Phase 7  |
| R11 | Subscriber confirmation abuse (spamming another person's inbox)                          | M   | H      | Double opt-in; rate limit on confirmation resend; "I did not request this" link; email-based rate limit                                    | Rate limit table records; high resend count              | Rate limit enforced; worst case: suspended subscriber                     | Phase 6  |
| R12 | Subscriber loses management token (cleared extension storage, reinstall)                 | H   | M      | Re-subscribe with same email → new management token issued; old token rotated                                                              | User reports inability to manage subscription            | Re-subscribe flow; no data lost from D1                                   | Phase 9  |
| R13 | Duplicate cron execution (platform scheduling quirk)                                     | L   | M      | Deterministic event IDs; UNIQUE(reset_cycle_id, type) in D1; INSERT OR IGNORE                                                              | Audit log shows duplicate attempt rejected               | No action; database constraint handled it                                 | Phase 8  |
| R14 | Partial delivery failure (some recipients sent, some not)                                | M   | M      | Per-row delivery state; retry only pending rows; never re-send rows already accepted by the provider                                       | Delivery table shows mixed states                        | Reschedule the existing row; terminal failures become `failed_permanent`  | Phase 7  |
| R15 | Rate-limit HMAC secret or token material compromised                                     | L   | H      | Store only SHA-256 token hashes; bound active management tokens; keep rate-limit HMAC secret outside Git                                   | Security audit; suspicious access log                    | Rotate `RATE_LIMIT_SECRET`, revoke active token rows, and issue new links | Phase 6  |
| R16 | D1 migration failure on deploy                                                           | L   | C      | Dry-run migration in CI; staging environment migration first; rollback guide                                                               | CI fails; deploy blocked                                 | Fix migration SQL; redeploy                                               | Phase 2  |
| R17 | Chrome Extension ID differs between dev and production (CORS mismatch)                   | H   | H      | Allowlist both IDs; environment-specific CORS config; document in Phase 1A spike                                                           | API returns CORS error in production extension           | Update CORS_ALLOWED_EXTENSION_IDS with production ID                      | Phase 1A |
| R18 | WhatsApp integration introduces per-message cost in future                               | L   | M      | Architecture designed for pluggable channels; WhatsApp adapter isolated                                                                    | Product decision at integration time                     | No action in MVP; document cost model before adding                       | Post-MVP |
| R19 | Unofficial source attribution legal/branding risk                                        | M   | M      | Clear "unofficial community tool" label; no logo copying; source attribution in all emails and UI; robots.txt 404 noted                    | User complaint; DMCA notice                              | Respond promptly; remove infringing content; add more disclaimers         | Phase 11 |
| R20 | Worker CPU timeout for large dispatch batch                                              | L   | H      | Send deliveries in small batches; use background queues (Queues) if needed; separate dispatch from crawl in same cron cycle                | Worker timeout error in logs                             | Split batch; use Queues API (free tier: 1M operations/month)              | Phase 7  |
| R21 | Source returns probability=null during partial degradation                               | M   | M      | sourceHealth=degraded allows status publish with non-null probability if previous is available; sourceHealth=unavailable suppresses update | Validation logic catches null probability                | Keep last valid status; log warning                                       | Phase 3  |
| R22 | Lifecycle detected incorrectly (announced vs completed conflated)                        | L   | H      | Explicit field separation in domain model; RESET_COMPLETED uses latestResetAt change not hoursSinceReset                                   | Test matrix L3, L4, L5 cover this                        | Hotfix detection logic; idempotent event ID prevents re-send              | Phase 4  |

---

## P0 Risks (Must Mitigate Before Phase 1)

| Risk                           | Status                                                            |
| ------------------------------ | ----------------------------------------------------------------- |
| R06 — D1 feature limitations   | MITIGATED in Phase 0.5: schema designed to D1/SQLite capabilities |
| R17 — Chrome Extension CORS    | ADDRESSED in Phase 1A spike                                       |
| R16 — Migration failure        | MITIGATED: CI dry-run mandatory                                   |
| R22 — Lifecycle detection flaw | RESOLVED: domain model corrected in Phase 0.5                     |

All other risks are accepted and tracked with mitigations in the appropriate phase.
