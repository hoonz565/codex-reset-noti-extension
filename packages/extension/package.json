{
  "name": "@codex-reset/extension",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "build": "esbuild src/popup.ts --bundle --outfile=dist/popup.js && node -e \"const fs=require('fs'); fs.mkdirSync('dist', {recursive:true}); fs.copyFileSync('manifest.json', 'dist/manifest.json'); fs.copyFileSync('src/popup.html', 'dist/popup.html');\""
  },
  "dependencies": {
    "@codex-reset/shared": "*"
  },
  "devDependencies": {
    "esbuild": "^0.21.5",
    "jsdom": "^29.1.1",
    "puppeteer": "^25.3.0",
    "vitest": "^1.6.1"
  }
}
