{
  "name": "@codex-reset/worker",
  "version": "1.0.0",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "test": "vitest run",
    "db:migrate:local": "wrangler d1 migrations apply DB --local",
    "db:migrate:staging": "wrangler d1 migrations apply DB --remote --env staging",
    "db:reset:local": "wrangler d1 execute DB --local --command=\"DROP TABLE IF EXISTS subscribers; DROP TABLE IF EXISTS reset_cycles; DROP TABLE IF EXISTS source_snapshots; DROP TABLE IF EXISTS reset_events; DROP TABLE IF EXISTS notification_deliveries; DROP TABLE IF EXISTS rate_limit_records; DROP TABLE IF EXISTS audit_events; DROP TABLE IF EXISTS d1_migrations;\"",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "@codex-reset/shared": "*"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.21.3",
    "@cloudflare/workers-types": "^5.20260718.1",
    "vitest": "^4.1.10",
    "wrangler": "^4.123.0"
  }
}
